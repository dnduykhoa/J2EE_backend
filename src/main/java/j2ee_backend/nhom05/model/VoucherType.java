package j2ee_backend.nhom05.model;

public enum VoucherType {
    SINGLE_USE, // Voucher dùng 1 lần duy nhất (sau khi 1 người dùng là hết)
    MULTI_USE   // Voucher dùng dài hạn (nhiều người có thể dùng, mỗi người 1 lần)
}
